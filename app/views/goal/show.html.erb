<h1>My Goals</h1>

<% @goals.each do |goal| %>
  <% if goal.user_id === @current_user.id %>
    <h3><%= goal.place.name %></h3>
    <h4><%= goal.description %></h4>
    <%= link_to 'destroy', goal, :method => :delete %>
  <% end %>
<% end %>



